/**
 * An object that can be rendered in a GL context.
 * @interface Renderable
 */

/**
 * Renders the object to the scene.
 * @method Renderable#render
 * @param {KraGL.Context} ctx
 *        The WebGL context used to render the object.
 */
